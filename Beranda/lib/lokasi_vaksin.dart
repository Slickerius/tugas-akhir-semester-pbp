import 'package:flutter/material.dart';

class LokasiVaksinPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
          child: Text('Lokasi Vaksin Page'),
        )
    );
  }
}

