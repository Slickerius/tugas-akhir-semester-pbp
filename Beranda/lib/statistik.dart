import 'package:flutter/material.dart';

class StatistikPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
          child: Text('Statistik Page'),
        )
    );
  }
}

